package com.github.pellaton.java9.leftpad.api;

/**
 * As we learned from npm every software needs a left pad library, so here it is.
 */
public interface LeftPad {

  String padLeft(String input, int targetLength);
  
}
